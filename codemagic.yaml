workflows:
  ios-surfshield:
    name: iOS SurfShield
    max_build_duration: 120
    instance_type: mac_mini_m2
    environment:
      ios_signing:
        provisioning_profiles:
          - "Distribution_Banners"
          - "Distribution_Adblocker"
          - "Distribution_Adbcloker"
          - "Distribution_Trackers"
          - "Distribution_Secure"
          - "Distribution_Basic"
          - "Distribution_Privacy"
          - "Distribution_Advanced"
        certificates:
          - "Distribution_Sertificate"
    integrations:
      app_store_connect: Surf Adbcloker Team
    scripts:
      - name: Set up keychain to be used for code signing
        script: |
          keychain initialize
          keychain add-certificates
      - name: Install provisioning profiles
        script: |
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          find "$CM_BUILD_DIR" -name "*.mobileprovision" -exec cp {} ~/Library/MobileDevice/Provisioning\ Profiles/ \;
          find "$CM_BUILD_DIR" -name "*.provisionprofile" -exec cp {} ~/Library/MobileDevice/Provisioning\ Profiles/ \;
      - name: Configure code signing
        script: |
          xcode-project use-profiles --project SurfShield.xcodeproj --archive-method app-store
      - name: Increment build version
        script: |
          # Создаем уникальную версию сборки на основе timestamp
          BUILD_NUMBER=$(date +%Y%m%d%H%M%S)
          echo "New build version: $BUILD_NUMBER"
          
          # Устанавливаем новую версию сборки для всех таргетов
          xcodebuild -project SurfShield.xcodeproj -target SurfShield -configuration Release CFBundleVersion=$BUILD_NUMBER
          xcodebuild -project SurfShield.xcodeproj -target adblocker -configuration Release CFBundleVersion=$BUILD_NUMBER
          xcodebuild -project SurfShield.xcodeproj -target basic -configuration Release CFBundleVersion=$BUILD_NUMBER
          xcodebuild -project SurfShield.xcodeproj -target advanced -configuration Release CFBundleVersion=$BUILD_NUMBER
          xcodebuild -project SurfShield.xcodeproj -target banners -configuration Release CFBundleVersion=$BUILD_NUMBER
          xcodebuild -project SurfShield.xcodeproj -target privacy -configuration Release CFBundleVersion=$BUILD_NUMBER
          xcodebuild -project SurfShield.xcodeproj -target secure -configuration Release CFBundleVersion=$BUILD_NUMBER
          xcodebuild -project SurfShield.xcodeproj -target trackers -configuration Release CFBundleVersion=$BUILD_NUMBER
          
          # Экспортируем новую версию для использования в сборке
          echo "CFBundleVersion=$BUILD_NUMBER" >> $CM_ENV
      - name: Build ipa for distribution
        script: |
          echo "Starting archive build..."
          echo "Using build version: $CFBundleVersion"
          xcodebuild archive -scheme SurfShield -project SurfShield.xcodeproj \
            -configuration Release \
            -archivePath "$CM_BUILD_DIR/build/SurfShield.xcarchive" \
            DEVELOPMENT_TEAM=9JXM4RZUCC \
            CODE_SIGN_STYLE=Automatic \
            CFBundleVersion=$CFBundleVersion \
            -allowProvisioningUpdates
      - name: Create export options plist
        script: |
          cat > "$CM_BUILD_DIR/export_options.plist" << EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>app-store-connect</string>
            <key>teamID</key>
            <string>9JXM4RZUCC</string>
            <key>uploadBitcode</key>
            <false/>
            <key>uploadSymbols</key>
            <true/>
            <key>compileBitcode</key>
            <false/>
            <key>signingStyle</key>
            <string>manual</string>
            <key>stripSwiftSymbols</key>
            <true/>
          </dict>
          </plist>
          EOF
      - name: Export ipa from archive
        script: |
          echo "Starting IPA export..."
          xcodebuild -exportArchive \
            -archivePath "$CM_BUILD_DIR/build/SurfShield.xcarchive" \
            -exportPath "$CM_BUILD_DIR/build" \
            -exportOptionsPlist "$CM_BUILD_DIR/export_options.plist" \
            -allowProvisioningUpdates
    artifacts:
      - build/*.ipa
      - build/SurfShield.xcarchive
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
      email:
        recipients:
          - arthurkulik99@gmail.com
        notify:
          success: true
          failure: false